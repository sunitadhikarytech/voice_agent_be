# Frontend (dashboard)

Placeholder for the browser dashboard. The client captures microphone audio and calls the
voice endpoints directly (the endpoint URL selects the pipeline — there is no smart router).

Built in the FE tickets: mic capture (VA-51), endpoint selection (VA-52), SSE captions
(VA-53), WebSocket audio playback (VA-54), barge-in (VA-55), debug panel (VA-56).
